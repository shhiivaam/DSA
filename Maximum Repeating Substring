class Solution {
public:
    int maxRepeating(string sequence, string word) {
        int k = 0;
        string cur = word;
        while (sequence.find(cur) != string::npos) {
            k++;
            cur += word;
        }
        return k;
    }
};
