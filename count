#include<bits/stdc++.h>
using namespace std;

void solution() {
    string s; cin >> s;
    int ans = 0;
    for (auto x : s) {
        ans += (x - '0');
    }
    cout << ans << endl;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    solution();
    return 0;
}
