class Solution {
public:
    int maxProduct(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int l = nums[nums.size()-1]-1;
        int r = nums[nums.size()-2]-1;
    return l*r;
    }
};
